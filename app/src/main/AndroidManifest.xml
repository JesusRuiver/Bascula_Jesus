<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ruiz.jesus.org.bascula_jesus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Frutas"
            android:label="frutas" />
        <activity
            android:name=".Refrescos"
            android:label="refrescos" />
        <activity
            android:name=".Verduras"
            android:label="verduras" />
        <activity
            android:name=".CancelaCompra"
            android:label="compra"
            android:theme="@android:style/Theme.Dialog"/>
        <activity android:name=".ContinuarCompra"/>
        <activity android:name=".Ticket"/>

    </application>

</manifest>